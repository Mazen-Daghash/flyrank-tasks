# Postgres connection (used when STORAGE=postgres, for local runs outside Docker)
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=tasksdb
DATABASE_URL=postgresql://app:app@localhost:5432/tasksdb

# Which repository to use: "postgres" or "memory"
STORAGE=postgres
